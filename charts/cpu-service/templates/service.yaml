apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpu-service.fullname" . }}
  labels:
    {{- include "cpu-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cpu-service.selectorLabels" . | nindent 4 }}
